const name = "Arbind Sah"

const percentage = ()=>{
    return `${Math.floor(Math.random()*100)}%`
}

const details = {
    name : "xyz",
    address:"ABC",
    phoneNum:9808977769
}

export {name,percentage,details};